# API Base URL - defaults to current hostname:8000 if not set
# In development: http://localhost:8000
# In production: will use the same domain as frontend with port 8000
VITE_API_BASE_URL=http://localhost:8000
